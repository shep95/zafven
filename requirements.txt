discord.py[voice]>=2.4.0
# Two-way voice (lets her hear/receive audio); mainline discord.py can't.
# This package only ships pre-release builds, so pin an exact alpha version.
discord-ext-voice-recv==0.5.2a179
python-dotenv>=1.0.0
# Vedic astrology computations (Swiss Ephemeris).
pyswisseph>=2.10.3.2
# Gregorian <-> Chinese lunisolar calendar (zodiac + lunar birthday).
lunardate>=0.2.2
# Birthplace timezone lookup for precise Vedic charts.
timezonefinder>=6.2.0
# Procedural art for /sigil and frequency portraits.
Pillow>=10.0.0
# Fetch YouTube transcripts for /learn knowledge reports.
youtube-transcript-api>=0.6.2
# aiohttp ships with discord.py and powers the Gemini gateway — pinned for clarity.
aiohttp>=3.9.0

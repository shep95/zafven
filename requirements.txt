# Discord library with voice support (PyNaCl). The experimental voice-*receive*
# extension was removed — it swapped in an unstable alpha voice client that hung
# connections; speaking/music use the standard client, so any recent 2.x is fine.
discord.py[voice]>=2.5
# Crypto backend for the AEAD voice encryption modes.
cryptography>=42.0
python-dotenv>=1.0.0
# Fetch YouTube transcripts for /learn knowledge reports.
youtube-transcript-api>=0.6.2
# aiohttp ships with discord.py and powers the Gemini gateway — pinned for clarity.
aiohttp>=3.9.0
# Resolve YouTube / search queries to a streamable audio URL for the /play music
# player. Keep it fresh — YouTube changes often and yt-dlp ships frequent fixes.
yt-dlp>=2024.8.6
